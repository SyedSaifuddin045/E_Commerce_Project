const connection = require('../connect.js')

class Seller {
    Add(seller_name,seller_phone,seller_email,seller_password)
    {
        let sql = `
        
        `;
    }
}

module.exports = Customers;